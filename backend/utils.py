# backend/utils.py
# Shared utility functions


def load_image(path):
    """Load an image from a file path and return as numpy array."""
    pass


def preprocess_image(image):
    """Preprocess image for model input (resize, normalize, etc.)."""
    pass


def postprocess_masks(masks, original_size):
    """Resize and clean up predicted masks to match original image dimensions."""
    pass


def draw_results(image, boxes, masks, labels, scores):
    """Overlay bounding boxes, masks, and labels onto the image for visualization."""
    pass
